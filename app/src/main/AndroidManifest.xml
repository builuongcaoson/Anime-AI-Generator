<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:name=".common.App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.Main"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity android:name=".feature.processing.avatar.AvatarProcessingActivity"/>

        <activity android:name=".feature.pickAvatar.PickAvatarActivity"/>

        <activity
            android:name=".feature.crop.CropActivity"
            android:theme="@style/Theme.Crop" />

        <activity android:name=".feature.processing.batch.BatchProcessingActivity" />

        <activity android:name=".feature.credit.CreditActivity" />

        <activity android:name=".feature.tutorial.TutorialActivity" />

        <activity android:name=".feature.first.FirstActivity" />

        <activity
            android:name=".feature.preview.PreviewActivity"
            android:theme="@style/Theme.Preview" />

        <activity android:name=".feature.setting.SettingActivity" />

        <activity android:name=".feature.model.ModelActivity" />

        <activity android:name=".feature.style.StyleActivity" />

        <activity android:name=".feature.explore.ExploreActivity" />

        <activity android:name=".feature.result.art.ArtResultActivity" />

        <activity android:name=".feature.processing.art.ArtProcessingActivity" />

        <activity android:name=".feature.iap.IapActivity" />

        <activity android:name=".feature.main.MainActivity" />

        <activity
            android:name=".feature.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />

        </provider>

<!--        Detect Face-->

        <meta-data
            android:name="com.google.firebase.ml.vision.DEPENDENCIES"
            android:value="face" />

<!--        Detect Face-->

<!--        Firebase Cloud Messaging-->

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/tools_theme" />

        <service
            android:name=".service.FirebaseFCMService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

<!--        Firebase Cloud Messaging-->

<!--        Admob-->

        <meta-data
            android:name="com.google.android.gms.ads.flag.OPTIMIZE_INITIALIZATION"
            android:value="true"/>

        <meta-data
            android:name="com.google.android.gms.ads.flag.OPTIMIZE_AD_LOADING"
            android:value="true"/>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/key_application_id" />

<!--        Admob-->

    </application>

</manifest>